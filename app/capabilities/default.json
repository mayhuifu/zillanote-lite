{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The one window may call the app's own commands and listen to its events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
