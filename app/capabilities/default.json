{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Both windows may call the app's own commands, listen to its events, and be dragged.",
  "windows": [
    "main",
    "mini"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
